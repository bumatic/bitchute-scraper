# Core dependencies
requests>=2.28.0
pandas>=1.5.0
python-dateutil>=2.8.0

# Token extraction (minimal Selenium usage)
selenium>=4.10.0
webdriver-manager>=3.8.0

# Retry logic for API requests
retrying>=1.3.0

# Data export formats
openpyxl>=3.0.0        # For Excel export
pyarrow>=10.0.0        # For Parquet export (optional)

# Concurrent processing
concurrent.futures      # Built-in Python 3.2+

# Development and testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-mock>=3.10.0

# Performance and monitoring (optional)
psutil>=5.8.0

# Configuration (optional)
pyyaml>=6.0

# Documentation (optional)
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0

# Removed dependencies (no longer needed):
# - beautifulsoup4 (no HTML parsing)
# - markdownify (no HTML to markdown conversion)
# - tqdm (simplified progress tracking)
# - aiohttp, aiofiles (using requests + threading instead)

# Backward compatibility note:
# If you need the old HTML-based scraper, you can install:
# beautifulsoup4>=4.11.0
# markdownify>=1.0.0